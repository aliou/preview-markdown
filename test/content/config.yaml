# Application Configuration

app:
  name: "Test Application"
  version: "1.0.0"
  debug: true

database:
  host: localhost
  port: 5432
  name: testdb
  
features:
  - authentication
  - logging
  - caching

settings:
  timeout: 30
  retries: 3
