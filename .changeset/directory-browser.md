---
"preview-markdown": minor
---

Add directory browser mode. Running `pmd` or `pmd <dir>` opens a TUI file browser with keyboard navigation, live filtering, relative timestamps, and seamless transition into the pager when a file is selected.
