---
"preview-markdown": minor
---

Replace the bundled Jellybeans themes with Senzu (regular dark and light variants). Default dark/light themes are now `senzu-dark` and `senzu-light`. The home-manager module options `dark`/`light` (raw color palettes) are replaced by `darkTheme`/`lightTheme` theme-name strings.
